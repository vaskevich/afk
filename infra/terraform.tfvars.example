# Copy to terraform.tfvars (gitignored) and fill in.

# region                 = "us-west-2"
# aws_vault_profile      = "osv_im_admin"
# aws_terraform_role_arn = ""

# Bucket names are global across all of Lightsail/S3, so "afk-sessions" itself is
# almost certainly taken. Pick something unique, e.g. your handle or account id.
bucket_name = "afk-sessions-osv-im"
